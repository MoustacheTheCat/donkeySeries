{% extends 'base.html.twig' %}

{% block title %}{{page_title}} Donkey episodes{% endblock %}

{% block body %}


<div class="container-fluid">
    <h1>{{page_title}}</h1>

    <div class="row">
        <div class="col-md-12">
            <table class="table table-striped table-hover">
                <thead>
                <tr>
                    <th>Program</th>
                    <th>Episode</th>
                    <th>Title</th>
                    <th>Season</th>
                    <th>Synopsis</th>
                </tr>
                </thead>
                <tbody>
              
                        <td>{{program}}</td>
                        <td>{{episode.number}}</td>
                        <td>{{episode.title}}</td>
                        <td>{{season}}</td>
                        <td>{{episode.synopsis}}</td>
                        {# <td>
                            <a href="{{path('admin_episode_edit', {'id': episode.id})}}" class="btn btn-primary">Editer</a>
                            <a href="{{path('admin_episode_delete', {'id': episode.id})}}" class="btn btn-danger">Supprimer</a>
                        </td> #}
                    </tr>
               
                </tbody>
            </table>
            {# <a href="{{path('admin_episode_add')}}" class="btn btn-success">Ajouter un épisode</a>#}
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
                 
        </div>
    </div>
</div>
{% endblock %}
{% extends 'base.html.twig' %}

{% block title %}{{page_title}} - Donkey Series{% endblock %}

{% block body %}


<div class="container-fluid">
    <h1>{{page_title}}</h1>
    <ul>
        {% for category in categories %}
            <li><a href="{{path("app_category_show", { categoryName: category.name })}}">{{category.name}}</a></li>
        {% endfor %}
</div>
{% endblock %}
